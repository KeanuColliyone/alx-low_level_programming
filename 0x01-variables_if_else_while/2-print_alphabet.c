#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int = n;
char = l;
srand(time(0));
n = rand() % 26;
l = 'a' + n;
putchar(l);
putchar('\n');
return (0);
}

